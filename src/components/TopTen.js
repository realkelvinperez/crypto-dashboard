import LongCard from "../LongCard";

export default function TopTen() {

    // fetch all of the data from coin gecko loop through it and pass it to Long Card

    return (
        <div>
            <LongCard />
        </div>
    );
}
